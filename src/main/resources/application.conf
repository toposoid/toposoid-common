deployment = ${?DEPLOYMENT}

akka{
      loggers = ["akka.event.slf4j.Slf4jLogger"]
      loglevel = "ERROR"
      stdout-loglevel = "ERROR"
}

local{
    retryCallMicroserviceNum = 3
    TOPOSOID_AWS_CREDENTIAL_ACCESSKEY = ${?TOPOSOID_AWS_CREDENTIAL_ACCESSKEY}
    TOPOSOID_AWS_CREDINTIAL_SECRETKEY = ${?TOPOSOID_AWS_CREDINTIAL_SECRETKEY}
}
